> Технический журнал для следующей сессии продолжения разработки — решения,
> причины, грабли (не построчный diff, это видно в коде). Актуальное
> человекочитаемое «что реализовано» — в `README.md`. Хронология сессий —
> в конце, кратко. Периодически сжимается (сохраняя все факты/грабли,
> убирая повествовательность) — не терять эту практику.

# Архитектурные принципы и грабли

## FIFO-разнесение платежей (ChargeAllocation)
Единый механизм для гаражей (электричество) и счетов членов (взносы/пеня):
`accounting._reallocate_fifo(charges, payments)`, обёртки
`reallocate_garage_charges(garage)` / `reallocate_member_charges(account)`.
Вызывать явно после ЛЮБОГО добавления `Charge`/`Payment` — не автоматически.
Идемпотентна (пересчёт с нуля), безопасна для повторного вызова.
`balance()` (агрегатная сумма) от `ChargeAllocation` не зависит — отдельный
путь для точного остатка по датам (пеня, статусы в журнале начислений).

## CSV-импорт в мастере настройки — настраиваемый формат
`CsvImportProfile` (по записи на тип: people/garages) хранит `columns` —
JSON-список ключей в порядке файла председателя, переиспользуется. UI —
попап: чекбокс (есть/нет) + числовое поле позиции (не drag-and-drop —
проще, без JS-библиотек, работает без риска потери состояния). Обязательные
колонки — чекбокс `disabled` + скрытое поле с тем же именем (браузер не
шлёт значение задизейбленного чекбокса). Парсинг — `dict(zip(columns, cells))`,
код обращается по `values.get("full_name")`, не по позиции. Образец CSV
(`_build_template_csv`) строится из того же активного формата.
`_looks_like_header_row()` — совпадение ЛЮБОЙ ячейки первой строки (без
регистра) с подписью любой колонки каталога (не только первой ячейки).
Формат: запятая-разделитель, UTF-8 (`utf-8-sig` для BOM), числа только с
точкой (не путать с разделителем при экспорте из Excel RU-локали).
Гаражи: несколько строк с одним номером = несколько собственников;
отсутствующий человек создаётся по ФИО (`_find_or_create_person_by_name`,
без регистра). Импорт идемпотентен: существующие гаражи/связи не
дублируются, только обновляют долю.

## Мастер первоначальной настройки (`/setup`, только председатель)
Оркестрация, не дублирование: там, где уже есть полноценная страница
(`garages.create`, `governance.term_detail`) — только точка входа, не
свои формы. Свои формы — только для того, чего не было вообще (CSV-импорт;
короткие формы кооператив/счёт/тариф/поставщик/счётчик). Статус шага —
вычисляется по данным (`wizard_status()`), не персистентный флаг — можно
открывать в любом порядке без риска рассинхронизации. Первый созыв
правления — `board_init()` создаёт `BoardTerm` с `elected_by_meeting_id=None`
напрямую (в обычном режиме `create_term()` требует протокол — на старте
избирать было не из чего, это не баг). Грабли: председатель из `seed.py` —
`person_id=None`, `sync_user_role()` не находит `User` при обычном
применении состава → `board_link_me()` — быстрая привязка на шаге правления.

## Новости — markdown + вложения (`news_format.py`, `News`, `NewsAttachment`)
Упрощённый markdown (`Markdown` + `nl2br`) → `bleach.clean()` (без `img`/
`script` — картинки идут отдельно через `NewsAttachment`) → `bleach.linkify()`.
Превью (`excerpt()`) — отдельный проход через `plain_text()` (markdown→HTML→
strip), не обрезка готового HTML (риск порвать тег). `news_format.py` не
завязан на модель News — переиспользован для вики (см. хронологию, сессия 34).

## Alembic — общие практические заметки (SQLite)
- Batch-mode обязателен для ALTER; `naming_convention` на `Base.metadata`
  даёт автоимена FK при автогенерации.
- `ADD COLUMN NOT NULL` без дефолта падает на существующих строках —
  дописывать `server_default=sa.false()`/аналог руками, автогенерация не
  добавляет сама.
- `DROP COLUMN` в batch — просто, имени constraint'а не требует (в отличие
  от FK).
- Расхождение веток (два `down_revision`) — не merge-ревизия, а
  перебазировка одной поверх другой (линейная история).
- Каждая миграция: `upgrade head → alembic check → downgrade base →
  upgrade head`, по возможности на БД с реальными (не пустыми) данными.
- История миграций один раз сбрасывалась до baseline-ревизии (по прямой
  просьбе, пока проект не был на боевых данных) — больше так делать нельзя.
- Частичный уникальный индекс («уникально только среди активных записей»,
  чтобы архивная запись и её преемник могли законно делить один и тот же
  «человекочитаемый» идентификатор — номер счёта, код и т.п.): обычный
  `UniqueConstraint`/`unique=True` на колонке для этого не годится (видит
  все строки). Вместо него — `Index(..., unique=True, sqlite_where=text(...),
  postgresql_where=text(...))` в `__table_args__` модели; в миграции —
  `op.create_index(..., unique=True, sqlite_where=...)` ОТДЕЛЬНО от
  `batch_alter_table` (создание индекса в SQLite не требует батч-режима,
  батч нужен только чтобы ДРОПНУТЬ старый обычный constraint). См.
  `MemberAccount.is_archived`/`account_number` — раздел 39 хронологии.
- Стрелевая посторонняя правка `cooperative.garage_area` вылезает в
  КАЖДОМ `alembic revision --autogenerate` этой ветки разработки (колонка
  убрана из модели ещё в миграции `2a97e92adbab`, но в dev-БД сессии
  почему-то ещё жива) — вырезать её из `upgrade()`/`downgrade()` вручную
  перед коммитом, она не относится к текущей задаче.

## Автоначисление пени (`penalty.py`)
Формула: непогашенный остаток × ключевая ставка ЦБ × 1/300 (первые 30 дней)
/ 1/150 (с 31-го) × дни, посуточно, по реальным датам платежей (через
`ChargeAllocation`). `Charge.penalty_calculated_through` делает повторный
вызов на ту же (или более раннюю) дату почти no-op — считает только новые
дни. Персистентные `Charge`-записи нужны (не виртуальное число) — иначе
нечем платить и нечего печатать в ПД-4. **Вызывается ТОЛЬКО раз в месяц по
cron** (`scripts/accrue_penalty.py`, см. «Крон-скрипты пени» ниже) — раньше
вызывалась ещё и тихо на каждом открытии `/dashboard`/`/finance/penalty/`,
от этого отказались (сессия 66): начисление было неочевидно для правления
(просто зашёл на сайт — что-то начислилось) и раздувало историю начислений
почти на каждый день при частых заходах. Кнопка «Пересчитать на дату» (была
для прошлой даты/отчётов) убрана как рудимент раньше в этой же сессии (62)
— для отчётов есть отдельная непишущая функция
(`compute_charge_penalty_breakdown`, см. «Расчёт пени для суда»,
хронология 56). **Известное ограничение**: задним числом внесённый платёж
ПОСЛЕ расчёта пени за эти дни не пересчитывает её ретроактивно, только
продолжает считать вперёд.

## cbr.ru (ключевая ставка) — daily-дамп, не список изменений
SOAP `DailyInfoWebServ/DailyInfo.asmx`, метод `KeyRate` — запись на КАЖДЫЙ
день. Сжимается в `penalty._compress_to_change_points()` (на входе) и
`compact_key_rates()` (по сохранённой истории). `is_manual=True` не
сжимается/не удаляется. Ответ — diffgram (настоящие XML-элементы, не
экранированная строка) — парсер ищет `DT`+`Rate` по всему дереву без
привязки к namespace. Подтверждено, что живой запрос реально работает
(сессия 58) — отдаёт ставку на каждый календарный день.

## Права доступа — только через permissions.py
`is_board()`/`is_chairman()`/`is_privileged()`/`is_owner_or_board()`/
`can_view_member_account()` — единственное место проверки ролей. Первые три
пробрасываются в Jinja через `_inject_user()`. **Грабли с затенением**: не
кэшировать `is_board()` в переменную с тем же именем
(`{% set is_board = is_board() %}`) — `{% include %}` передаёт контекст
целиком, внутри инклюда вызов ожидает функцию, получает bool →
`TypeError: 'bool' object is not callable`. Ловится только реальным
рендером (не статическим Jinja-парсингом) — этот баг находили дважды
независимо (`garages/detail.html`, `pd4/select.html`) и на будущее нужен
e2e-рендер под ролями по ВСЕМ блюпринтам, включая второстепенные.
`roles_required`/`ROLE_LEVEL` (auth.py) — доступ к роуту целиком (декоратор,
минимальная роль); `permissions.py` — доступ к объекту внутри роута (свой
это гараж/счёт или нет). Разные уровни, не путать. `is_privileged()`
(председатель/бухгалтер) — отдельный, более узкий набор ролей, НЕ
выражается через `ROLE_LEVEL` (BOARD и ACCOUNTANT на одном уровне доступа)
— нужен, где рядовому члену правления видеть/трогать что-то ещё не
положено (списание/массовое списание пени и т.п.).

## Правление и созывы
`governance.apply_term()` — явная кнопка, НЕ авто при добавлении членов
(иначе председатель, заполняющий состав, рискует потерять флаг у себя
самого на середине). Бухгалтер — НЕ через созыв: назначается председателем
напрямую (`/governance/accountant/set|unset`), может не быть членом
правления, применяется сразу (риска самоблокировки нет).

## Searchable select — рендерится в <body>, не внутри формы
`base.html: enhanceSearchableSelect()` — общий JS для `<select data-searchable>`.
Список — прямой потомок `document.body`, `position: fixed` (координаты от
`getBoundingClientRect()`, пересчёт на scroll/resize) — не внутри
`position-relative`-обёртки, т.к. `modal-dialog-scrollable` ставит
`overflow-y:auto` на `.modal-body`, что обрезает абсолютно позиционированных
потомков независимо от z-index. Прячется на `hidden.bs.modal`. Глобальная
(`window.enhanceSearchableSelect`) — вызывать вручную для динамически
добавляемых `<select>`.

## Прочие устоявшиеся соглашения
- `cooperative_balance()` (сумма `BankAccount.balance`, реальные деньги) ≠
  `receivables_balance()` (сумма лицевых счетов членов, кто сколько должен)
  — не путать; на дашборде и `/cooperative/` показывается только первое
  (сводка долгов на дашборде, сессия 67, — отдельная агрегатная выборка по
  `MemberAccount`, не `receivables_balance()`).
- Приватность лицевых счетов — `can_view_member_account()` фильтрует
  построчно (сводка на странице гаража, счета членов) в РОУТЕ (Python), не
  в шаблоне — рядовой собственник видит только свой счёт, не содольщика.
- Модалки — `modal-lg` по умолчанию.
- Верификация после каждой правки: `py_compile`, Jinja2-парсинг всех
  шаблонов, статическая сверка `url_for` с маршрутами, сверка `_()`-строк
  с `i18n.py` (+AST-проверка дублей ключей), и e2e через
  `app.test_client()` под разными ролями — статический разбор не ловит
  затенение переменных или неверные дефолты роутов.

## Разбор дробного ввода по локали (`app.i18n.parse_decimal`)
`fmt2()` выводит дробные числа с запятой (ru-локаль) — значит и вводить их
нужно уметь с запятой, а не только с точкой. Голый `Decimal(request.
form[...])` по всему проекту заменён на `parse_decimal`/
`parse_optional_decimal`: последний ВСТРЕТИВШИЙСЯ разделитель (","/".")
считается дробным (правильно разбирает британский формат "1,234.56" —
запятая тысяч, точка дробная часть); один и тот же разделитель, повторённый
НЕСКОЛЬКО раз ("1,234,567") — точно разделитель тысяч, убирается целиком;
пробелы (в т.ч. неразрывные — копипаста сгруппированных по тысячам чисел)
убираются всегда. Однозначного решения для ОДИНОЧНОЙ запятой/точки без
второго разделителя нет в общем случае (британские "1,234" без точки — это
1234, не 1.234) — трактуется как дробный разделитель, соответствует
реальным форматам интерфейса (ru — запятая, en — точка) без группировки
тысяч. Клиент: денежные/дробные `<input type="number">` переведены на
`type="text" inputmode="decimal"` — нативный number-инпут молча блокирует
ввод запятой в зависимости от языка БРАУЗЕРА (не локали самого сайта);
целочисленные поля (год, месяц, число разрядов номера счёта, позиция в
реестре) остались `type="number"`. JS-превью, которые повторяют серверный
расчёт до отправки формы (массовое начисление, показания счётчика),
получили свой аналог разбора (`parseDecimalInput()` в `base.html`) — голый
`parseFloat()` останавливается на запятой и даёт неверный результат.

## Компенсирующие проводки — общий приём для «денег не двигаем, баланс поправляем»
Списание пени, зачёт между лицевыми счетами, унаследованный долг/переплата
при смене собственника, отмена зачёта — один и тот же приём: не трогать
баланс напрямую (не хранимое поле, считается как sum(payments)-sum(charges)),
а завести компенсирующий `Charge` (списание/источник) и/или `Payment`
(зачисление/получатель) с явным комментарием-причиной, затем
`reallocate_member_charges()`. Где нужна связь с человеком/начислением на
другой стороне — структурный FK (`related_person_id`, `penalty_for_charge_id`,
`offset_charge_id`, `reverses_payment_id`), не парсинг ФИО из текста
комментария — комментарий остаётся человекочитаемым для печати, но ссылки
(`linkify_related_person` и т.п.) строятся из поля, не строки.

## Протухшие relationship-коллекции после delete()+flush() — не только тестовая грабля
Если код (а) читает relationship-коллекцию объекта в локальную переменную
(`charges = list(account.charges)`), (б) удаляет часть её элементов через
`session.delete(item)` НАПРЯМУЮ (не `collection.remove(item)`), затем (в) в
том же запросе читает ТУ ЖЕ relationship-коллекцию ещё раз (например,
следующий вызов `reallocate_member_charges()` читает `account.charges`) —
коллекция НЕ обновляется сама по себе после `flush()` (flush не трогает уже
загруженные в память коллекции), по-прежнему возвращает только что
удалённые объекты. Следующая попытка вставить строку, ссылающуюся на такой
объект (например `ChargeAllocation.charge_id`), падает с `IntegrityError:
FOREIGN KEY constraint failed`. Это НЕ только артефакт общей pytest-сессии
(см. следующий пункт) — воспроизводится и в обычном production-запросе с
одной штатной сессией (найдено на реальном использовании массового удаления
пени, не тестами — см. хронологию 63). Фикс — явный
`database.db_session.expire(obj, ["имя_relationship"])` между удалением/
flush и повторным чтением той же коллекции.

## Тесты: общая SQLAlchemy-сессия между setup-кодом и `client.post(...)`
Фикстура `db` — ОДНА сессия и на код настройки теста, и на сам Flask-роут
под тестом (в реальном использовании — разные сессии на запрос, см. выше).
Если тест ЧИТАЕТ что-то через relationship ДО `client.post(...)`, который
внутри роута удаляет/пересчитывает те же объекты — тестовая сессия видит
протухшую коллекцию в момент, когда роут читает её заново (тот же механизм,
что и в проде, просто гарантированно происходит на маленьких тестовых
данных, где в реальном использовании совпадение было бы редким). Симптом —
`IntegrityError` внутри `client.post(...)`, которого при обычной работе
сайта не было бы (`db_session.remove()` на teardown запроса создаёт свежую
сессию каждый раз). Фикс в тесте — `db.expire_all()` сразу перед мутирующим
`client.post(...)`; встречалось многократно, повторяется в новых тестах,
если про это забыть.

## Электронное голосование (`voting.py`)
Вес голоса = сумма долей по ВСЕМ гаражам человека
(`voting.person_voting_weight`), не «бюллетень на гараж» — `VoteBallot`
одна запись на (question, person). Кворум — жёсткое правило (не
настраиваемое поле): >50% от общего веса кооператива
(`QUORUM_THRESHOLD=Decimal("0.5")`, `total_cooperative_weight()`). Порог
принятия по вопросу (`VoteQuestion.majority_threshold`, настраиваемый)
считается ВСЕГДА от общего числа голосов кооператива, не от
проголосовавших по вопросу — специально убран альтернативный знаменатель.
`question_results()` дополнительно проверяет `quorum_met()`. Статусы:
draft (не виден рядовым) → open (upsert по question_id+person_id,
переголосование разрешено) → closed (явная кнопка председателя; приём
прекращается и при истечении `closes_at`, даже без кнопки). Протокол —
обычный `Document`, не автогенерируется. Права: создание/повестка/
открытие/закрытие — только председатель; черновики видны только правлению;
результаты по вопросам — рядовым только после закрытия. Голосовать может
только человек с весом > 0.

**VoteType.IN_PERSON** (третий вид, наряду с absentee/in_person_and_absentee)
— решение уже принято на собрании, электронная повестка не заводится:
`Vote` создаётся сразу в CLOSED (`opens_at == closes_at` = дата собрания),
`Document` с протоколом создаётся и привязывается атомарно.
**`set_ballot_for_person()`** — председатель вручную записывает бюллетень
за члена (только RoleEnum.CHAIRMAN); работает только для
IN_PERSON_AND_ABSENTEE и только пока OPEN; переиспользует `cast_ballots()`
— последующее самостоятельное голосование участника корректно перезаписывает
запись председателя (UniqueConstraint(question_id, person_id), последняя
подача побеждает). В отличие от бюллетеня участника, для председателя
разрешена частичная запись. `eligible_voters()` — люди с весом > 0,
отсортированы по ФИО.

**Публичный комментарий к голосу** (`VoteBallot.comment`/
`VoteProposalBoardBallot.comment`) — сознательно НЕ гейтится так же, как
агрегированные итоги (видны правлению всегда, рядовым — только после
закрытия/решения): комментарий виден ВСЕМ сразу, независимо от статуса —
это аргументация человека, а не результат, способный повлиять на ещё не
проголосовавших через бандвагон-эффект числа.

## Предложения голосований от рядовых членов (`app/proposals.py`)
`VoteProposal`/`VoteProposalBoardBallot`. Правление голосует «за/против/
воздержался» ПО ГОЛОВАМ (одна запись на person_id ТЕКУЩЕГО созыва,
`governance.current_board_member_ids()`), не по долям владения —
сознательно другая арифметика, чем у самого `Vote`. Решение подводится
ЛЕНИВО (`resolve_if_due`, вызывается со страниц списка/карточки и сразу
после голоса члена правления) — без планировщика/cron, тот же приём, что
`voting.is_accepting_ballots`. Условие: проголосовали все члены текущего
созыва ИЛИ истекла неделя (`PROPOSAL_REVIEW_PERIOD`) с подачи. Ничья (в т.ч.
0:0) — отклонение. При одобрении сразу создаётся `Vote` в DRAFT (ABSENTEE,
14 дней) с тем же title/description. **Грабля с relationship-кешем**: после
`db.add()` нового `VoteProposalBoardBallot` в том же запросе
`proposal.board_ballots` не подхватывает свежую запись, если коллекция уже
была загружена раньше в этой же сессии (см. принцип «Протухшие
relationship-коллекции» выше — тот же класс проблемы) — обошли:
`resolve_if_due`/`proposal_tally` всегда бьют в БД напрямую
(`db_session.query(...)`), не через relationship.

## Крон-скрипты пени: ставка ЦБ + начисление
`scripts/update_key_rate.py` — standalone (сам `create_app()`+
`app_context()`), тянет с cbr.ru от дня после последней записи (или с
13.09.2013, если БД пустая) по сегодня, использует существующие
`fetch_key_rates_from_cbr`/`save_key_rates`/`compact_key_rates`. Раз в
сутки (cron). `scripts/accrue_penalty.py` (сессия 66) — тот же
standalone-приём, вызывает `accrue_penalties(today)` напрямую; раз в
месяц, по времени чуть позже `update_key_rate.sh` (чтобы ставка на начало
месяца уже была подтянута заранее). Обе `.sh`-обёртки — общий приём: лог в
`instance/logs/`, `flock`, автоопределение venv (`.venv`/`venv`).

## Права на печать ПД-4 — разделены по видам платёжек, не по ролям целиком
Правление печатает ТОЛЬКО взносы (MemberAccount) — электричество
(PersonalAccount) правление не печатает вообще, даже подставив garage_id
вручную (`_print_electricity_slip()` требует реального владения, без
исключения для is_board()). Рядовой член печатает ЛЮБЫЕ свои платёжки
(взносы+электричество) по гаражам, которыми владеет хоть частично.
`_electricity_account_view()`-адаптер даёт PersonalAccount интерфейс
MemberAccount — шаблоны печати не знают о разнице типов.
**Важно**: электричество выставляется на гараж целиком (общий счётчик) —
частичный собственник видит и может оплатить весь остаток, не только
свою долю (не баг, так было всегда, просто доступно теперь любому
частичному собственнику).

## API банка (`app/bank_api/`, `app/bank_sync.py`) — реализован только Сбербанк
`BankAccount.api_provider` (none/sberbank/vtb/tbank) — ВТБ/Т-Банк
выбираемы в UI, но `get_client()` для них всегда `None`.

0. **Авторизация — refresh_token, не client_credentials** (подтверждено
   документацией). Все запросы — от имени конкретного пользователя
   СберБизнес: access_token (60 мин, не хранится) + refresh_token (180
   дней, зашифрован в `BankApiCredential.refresh_token_encrypted`).
   Председатель вводит refresh_token в модалке «Настроить API» (access_token
   не нужен — обновляется сам, см. `SberbankClient._get_access_token`).
   **Ротация**: банк может вернуть новый refresh_token, старый
   аннулируется — `SberbankClient.rotated_refresh_token`; КАЖДЫЙ роут в
   `bank_sync.py`, вызывающий `get_client()`, обязан вызвать
   `_persist_rotated_refresh_token(cred, client)` перед `commit()`,
   включая путь ошибки (refresh мог пройти даже если бизнес-запрос упал).
1. **mTLS обязателен для TLS-соединения**, не только токен в заголовке.
   .pfx/.p12 из ЛК Sber API + пароль → PEM (`cryptography.pkcs12`),
   хранится в `BANK_CERTS_FOLDER` (НЕ `UPLOAD_FOLDER` — нет отдающего
   роута), права 0o700/0o600. Исходный .pfx/пароль не сохраняются.
   **.p12 содержит ЦЕПОЧКУ сертификатов** — `_save_client_cert` сохраняет
   всю (`extra_certs`), не только лист, иначе mTLS-рукопожатие может не
   пройти.
2. **Доверенные корни — УЦ Сбера И НУЦ Минцифры** (обе, подтверждено
   developers.sber.ru/docs/ru/sber-api/start/tls) — обычный certifi-бандл
   не знает → `SSLError: self-signed certificate in certificate chain`.
   Банк отдаёт готовую цепочку по прямой ссылке (см. `Config.SBERBANK_API_CA_BUNDLE`,
   `.env.example`).
3. **Тестовый контур**: `https://fintech-test.sberbank.ru:9443` (подтверждено
   ЛК кооператива), НЕ `edupirfintech.sberbank.ru` (был баг первой версии).
   `token_url` теперь выводится ИЗ `base_url`
   (`{base_url}/ic/sso/api/v2/oauth/token`), не захардкожен на промышленный
   хост.
4. **Официальный перечень scope-операций не содержит реестра начислений**
   (`PAYMENTS_REGISTRY` есть, для начислений — нет) — возможно, отдельный
   продукт, не проверено ни в одну сторону. Ручной путь (CP1251-файл через
   веб-интерфейс СберБизнес Онлайн) не зависит от API.
5. **Реестры — CP1251-текст, настраиваемый формат** (`BankRegistryFormat`,
   тот же принцип, что CSV-импорт — каталог полей + чекбокс/позиция, см.
   `bank_api/registry_file.py: CHARGE_FIELD_CATALOG/PAYMENT_FIELD_CATALOG`).
   Дефолт — **реальный** формат по образцам от председателя: реестр
   начислений — 5 полей (`лицевой_счёт;ФИО;назначение;код_услуги;сумма`),
   точка как разделитель, без заголовка; реестр платежей — 13 полей,
   запятая как разделитель, плюс строка-сводка (`trailer_prefix`,
   распознаётся и пропускается). **Сумма начисления vs сумма зачисления —
   разные поля** (`PaymentRegistryEntry.amount` vs `.credited_amount`/
   `.fee_amount`) — комиссия банка не должна восприниматься как недоплата.
   `build_registry_format()` — единственное место сборки формата из БД,
   для API и для ручного скачивания/загрузки одинаково.

Ручной путь (скачать/загрузить CP1251-файл через веб-интерфейс банка)
всегда доступен независимо от API — сама механика передачи реестров
именно через API не подтверждена. **Вся интеграция с банком и cbr.ru не
проверялась живым запросом** из песочницы разработки; эндпоинт
access_token тоже не подтверждён (`SBERBANK_API_TOKEN_URL`).

Реестр начислений строится из ТЕКУЩЕЙ задолженности (`balance() < 0`), не
из списка `Charge` за период — известное ограничение: нет отслеживания
«уже в реестре», повторная отправка шлёт всё заново (дублей в кооперативе
не создаёт — это витрина для банка).

**Выписка тоже гасит долг автоматически.** Банк нередко вписывает номер
лицевого счёта в свободный текст назначения обычной выписки, например
«ЛС 10640; ЧЛЕНСКИЕ ВЗНОСЫ...» — `bank_sync.extract_account_number()`
(regex `лс\s*[:№]?\s*(\d+)`, без регистра) распознаёт при `sync_statement`;
при совпадении с существующим счётом **зачисление** (не списание)
автоматически заводит `Payment` на полную сумму и разносит по FIFO без
подтверждения — так и запрошено (в отличие от реестра платежей, где
разнесение — ручная кнопка «Разнести»). Общий код —
`bank_sync._allocate_payment_to_account()`. Комиссия банка в выписке
отдельным полем не приходит — вся зачисленная сумма гасит долг как есть.

**Если номера нет — поиск по ФИО, включая лиц для связи.**
`_resolve_account_by_name()`: полное ФИО плательщика → форма «Фамилия И.О.»
(банк часто указывает того, ЗА КОГО платят, не плательщика) → среди ВСЕХ
`Person`, включая `GarageContact` (лицо для связи поднимает счета
владельцев гаража — у контакта обычно нет своего счёта). Разносится
ТОЛЬКО при однозначном результате; при нескольких кандидатах — сужение по
`FeeType.name` в тексте назначения (`_narrow_by_fee_type`); если и после
этого не однозначно — остаётся неразнесённым (риск отнести чужой платёж
важнее автоматизации). Тот же путь и в ручном «Разнести».

## Вики (`app/wiki.py`, `WikiPage`) — справочные заметки, не хронологическая лента
В отличие от News (лента событий/объявлений) — набор страниц-справок,
организованных ДЕРЕВОМ разделов/подразделов (`WikiPage.parent_id`,
самоссылающийся FK, глубина не ограничена). Формат тела — тот же markdown,
что у новостей (`news_format.py` переиспользован, второй jinja-глобал
`render_wiki_html`). Создают/редактируют/удаляют только правление.
Видимость для ЧТЕНИЯ — `WikiPage.is_internal` ПО СТРАНИЦЕ (не по всему
разделу целиком и не общий флаг на всё), тот же принцип, что у
`Document.is_internal`. Пункт «Вики» — в основной навигации (дерево видно
любому вошедшему, просто отфильтровано).

**Дерево навигации** — `wiki._build_visible_tree()`: если скрытым сделан
ЦЕЛЫЙ раздел, а какой-то из его подразделов остался общедоступным — этот
подраздел поднимается к ближайшему видимому предку (или в корень), не
пропадает — иначе видимая страница стала бы недостижимой только из-за
того, что раздел над ней сделали внутренним («дыра» в дереве). Рендер —
`wiki/_tree.html`, рекурсивный Jinja-макрос + Bootstrap `collapse`, без
доп. JS.

**Удаление раздела с непустыми детьми запрещено** — explicit-проверка в
`wiki.delete()` + `ondelete="RESTRICT"` на FK как страховка. Выбор родителя
в форме — нельзя выбрать саму страницу/её потомка, проверяется на СЕРВЕРЕ
(`_descendant_ids()`), не только исключением из `<select>`.

## Inline-картинки в теле новости/вики (`NewsAttachment`/`WikiAttachment`)
AJAX-загрузка на лету (POST `/news|wiki/attachments/upload`, board-only,
белый список jpg/png/webp/gif) — файл грузится СРАЗУ по выбору, ссылка
`![](url)` вставляется в textarea в позицию курсора без ожидания
сохранения статьи. **Проблема курицы и яйца** — картинка получает id/URL
раньше, чем существует сама статья: `news_id`/`page_id` NULLABLE, при
AJAX-загрузке создаётся «осиротевшее» вложение (`is_inline=True` у News; у
Wiki отдельного `is_inline` не нужно — там ВСЕ вложения inline). При
сохранении статьи `_sync_inline_attachments(item, body_text)` регуляркой
находит id вложений в НОВОМ тексте, «забирает» из них те, что осиротевшие
И принадлежат текущему автору (`author_id` — чужое осиротевшее вложение по
ссылке в тексте не подхватывается, иначе можно «угнать» чужую загрузку);
вложения, ссылку на которые убрали при правке — удаляются автоматически
(с файлом на диске). **Удаление файла с диска** — общий SQLAlchemy
`after_delete`-listener (`app/models.py: _delete_attachment_file`, один
обработчик на оба типа вложений) — раньше файл на диске оставался висеть
при удалении через ЛЮБОЙ путь (чекбокс/каскад/cron), теперь один хук
покрывает все. **Видимость файла вики наследует видимость страницы**
(`wiki.attachment()` — 403 не-правлению, если `att.page.is_internal`; для
ещё не сохранённого осиротевшего вложения по умолчанию считаем его
внутренним). **Осиротевшие вложения чистит `scripts/cleanup_orphan_attachments.py`
по cron** (тот же `.sh`-паттерн: venv/flock/лог) — удаляет через ORM
(`db_session.delete`, чтобы сработал `after_delete`) вложения без родителя
старше 24 часов.


# Хронология сессий (сжато)

1. Электричество+Счёт гаража объединены в одну вкладку, реальная связь
   показание↔начисление (`Charge.reading_id`).
2. ПД-4 для электросчёта гаража (долг по общему счётчику).
3. FIFO-разнесение платежей введено впервые (гаражи/электричество).
4. Один платёж — одна строка в журнале начислений (через `ChargeAllocation`,
   не позиционный `zip_longest`).
5. Валидация показаний счётчика (запрет уменьшения); правка последнего
   показания — только председатель.
6. Аудит доступа — закрыты реальные дыры (чужие персоны/гаражи/фото/ПД-4
   без проверки владения); смена пароля, поиск в select'ах, чек-лист
   гаражей для массового начисления.
7. `cooperative_balance()` / `receivables_balance()` разделены; модуль
   расчётов с контрагентами.
8. Права доступа централизованы в `permissions.py`; `/garages/charges/add`
   слита в `/finance/mass-charge`.
9. Расчётные счета/баланс/комментарий кооператива скрыты от рядовых членов.
10. Созывы правления + ревизионная комиссия (`BoardTerm`/`BoardMember`),
    применение состава — отдельная кнопка.
11. Старые чекбоксы флагов правления убраны; `Cooperative.balance` — из
    ручного поля в вычисляемое.
12. Бухгалтер отделён от созыва правления (назначается председателем
    напрямую).
13. Автоматическое начисление пени — новый `penalty.py`, интеграция с
    ключевой ставкой ЦБ, FIFO обобщено на счета членов (самая насыщенная
    по граблям сессия, см. принципы).
14. Слияние параллельных веток (пеня + рефакторинг бухгалтера) вручную,
    миграция пени перебазирована.
15. Точечные правки: баланс кооператива, компактная пагинация, сжатие
    daily-дампа ЦБ, автопересчёт пени на лету, единообразные проверки прав
    (найден баг с затенением `is_board`), обрезка searchable-select в
    модалках, дефолт доли владения 1.
16. Электронное голосование (очно-заочное/заочное) + крон обновления
    ключевой ставки.
17. Права на печать ПД-4 пересмотрены (правление — только взносы, член —
    любые свои); найден и исправлен предсуществующий баг с затенением
    `is_board` на странице выбора счетов.
18. Главная страница переработана: форма входа в карточку справа, по
    центру — новостная лента (`News`, `/news`, запись только правлению);
    языковой/тематический переключатели — выпадающее меню с флагом-эмодзи
    и кнопка-пиктограмма.
19. История миграций сброшена до одной baseline-ревизии (по прямой
    просьбе, до боевых данных). Мастер первоначальной настройки (`/setup`)
    — чек-лист из 8 шагов, свои формы только там, где не было UI вообще;
    проверен полный e2e-цикл.
20. Слияние с параллельной сессией: markdown-разметка новостей + вложения
    (`NewsAttachment`, `/news/<id>`); baseline-миграция перегенерирована;
    единственный ручной конфликт — `auth/login.html`.
21. CSV-импорт в мастере стал настраиваемым (`CsvImportProfile`); проверено
    e2e с нестандартным форматом колонок.
22. Интеграция с API банка — `BankAccount.api_provider`,
    `app/bank_api/`, `app/bank_sync.py`, новые таблицы. Реализован только
    Сбербанк.
23. mTLS + НУЦ Минцифры + CP1251-формат реестров (исправление после ревью
    п.22).
24. Реальный формат файлов реестров (по образцам от председателя) +
    настраиваемость (`BankRegistryFormat`).
25. Автопогашение по номеру лицевого счёта в обычной выписке
    (`extract_account_number()`), без подтверждения.
26. Поиск лицевого счёта по ФИО и лицам для связи, когда номера в тексте
    нет (`_resolve_account_by_name` и вспомогательные).
27. Сводка лицевых счетов на странице гаража (фильтр `can_view_member_account()`);
    фильтр документов по типу — через `<select>`.
28. Живая проверка API Сбербанка на реальном контуре — независимые
    находки, подтверждены документацией/ЛК кооператива (см. принципы «API
    банка», п. 0–3): grant type refresh_token, вся цепочка сертификатов
    из .p12, тестовый хост `fintech-test.sberbank.ru:9443`, CA-бандл НУЦ
    Минцифры.
29. Улучшение распознавания номера счёта и разнесения на РЕАЛЬНОЙ выписке
    (81 зачисление, не синтетика): regex расширен под «Л/С»/«ЛСИ» (10→29
    из 81); агрегированные проводки по реестру платежей явно помечаются
    (`is_aggregate_registry_payment`, 22 из 81 — авто-разнесение для них
    скрыто); кнопка «Разнести всё возможное». Тест на
    `tests/fixtures/statement_sample.csv` (структура подлинная, ПД
    вымышленные) фиксирует достигнутую долю распознавания как
    регрессионный порог.
30. Скрипт восстановления БД (`scripts/restore_db.py`/`.sh`, симметричен
    `backup_db`) — `--list`/`--restore N|имя|latest`, pre-restore бэкап,
    `integrity_check`, атомарная запись через временный файл.
31. Точный AST/tokenize-экстрактор ключей i18n (`app/i18n.py`) — сверка
    ru/en. Найден и исправлен 1 реальный баг (непереведённое «ЛС
    <номер>»); добавлено 35 переводов. Итог: 1126 ключей, 0 дублей.
32. Новые типы документов (invoice/statement/certificate/estimate/report)
    + `Document.is_internal` (список скрывает от не-правления, `download()`
    — 403 при прямом обращении, закрыт IDOR).
33. Голосования: `VoteType.IN_PERSON` + `set_ballot_for_person()` +
    `eligible_voters()` (см. принципы «Электронное голосование»).
34. Вики (`app/wiki.py`, `WikiPage`) — справочные заметки, отдельно от
    новостной ленты (см. принципы «Вики»). Markdown переиспользует
    `news_format.py`. Видимость по странице. Миграция `3c06185c2e9e`.
35. `context.md` сжат (84→37 КБ). Полный AST-аудит i18n по всему проекту —
    найден и исправлен 1 дубль ключа (недосмотр сессии 34, вики),
    переведены 63 пропущенные строки (в основном IN_PERSON-голосование и
    земельный налог/массовое начисление). Итог: 1220 ключей, 0 дублей, 0
    непереведённых.
36. Inline-картинки в теле новости/вики (см. принципы). AJAX-загрузка на
    лету (выбор из 2 подходов), очистка осиротевших вложений через cron
    (`scripts/cleanup_orphan_attachments.py`, порог 24ч). Общий
    `after_delete`-listener на файл вместо трёх отдельных путей удаления.
    Миграция `3d0ecf5086aa`. Тесты — `test_inline_attachments.py` (10,
    включая защиту от «угона» чужого осиротевшего вложения). pytest 176.
37. Дерево разделов/подразделов в вики — плоская `category` заменена на
    самоссылающийся `WikiPage.parent_id` (см. принципы «Вики»). Решения по
    умолчанию: удаление раздела с детьми запрещено (RESTRICT + explicit-
    проверка); видимые потомки скрытого раздела поднимаются к ближайшему
    видимому предку. Миграция `e80abefa5b98` — бэкофилл `category`→корневые
    разделы на реальных данных; `downgrade()` восстанавливает колонку, но
    не контент. `tests/test_wiki.py` переписан целиком (16 тестов). pytest
    181.
38. Слияние разошедшихся копий проекта: архив от Sne (параллельная сессия
    — вики с деревом, eWeLink-заготовка, встречи, контрагенты,
    рейт-лимитинг, обновлённый CSV-мастер, земельный налог) взят за
    основу, поверх — только уникальные различия этого чата (regex «ЛСИ»).
    Миграция `36ebefe01ade` синхронизирует схему автогенерацией (в архиве
    `migrations/` не было вовсе) — предупреждение оставлено в самой
    миграции про проверку `alembic_version` перед накаткой на БД, где уже
    применялись оригинальные ревизии `3c06185c2e9e`/`3d0ecf5086aa`.
    Попутно исправлен несвязанный баг: `is_aggregate_registry_payment`
    вызывалась в `bank_sync.statement()`, но нигде не была определена —
    `NameError` на каждой выписке (в архиве не было ни одного теста на
    неё). `tests/fixtures/statement_sample.csv` очищен от реальных ПД
    (структура/регрессионные цифры 81/29/22 не изменились). pytest 184.
39. Собственник гаража, оставшегося вовсе без собственников — баг:
    `owner_index` считался по числу ТЕКУЩИХ `GarageOwnership`, после
    выбытия единственного собственника новому снова доставался индекс 0 и
    уже занятый архивным счётом номер → `UNIQUE constraint failed`. Фикс —
    `next_owner_index()`: индекс по максимуму среди ВСЕХ (в т.ч. архивных)
    счетов гаража.

    Расширено до сценария «наследство» — когда гараж ПОВТОРНО обзаводится
    собственником после того, как оставался вовсе без него:
    `garages._archive_owner_accounts_and_reuse` архивирует прежние счета
    (остаются на прежнем person_id для истории), новому заводит свежие
    счета С ТЕМИ ЖЕ номерами (намеренно — номера уже разошлись людям/в
    банковских реквизитах) и переносит остаток компенсирующими
    Charge/Payment (`accounting.transfer_member_account_balance`, см.
    принцип «Компенсирующие проводки»). Схема:
    `MemberAccount.account_number`/`(person_id, garage_id, fee_type_id)` —
    `UniqueConstraint` заменены частичными уникальными индексами (`WHERE
    NOT is_archived`, см. принципы «Alembic»). Миграция `5300dca34cc6`.

    Краевой случай на реальных данных (гараж №36): удаление и тут же
    повторное добавление ОДНОГО И ТОГО ЖЕ человека как собственника — тот
    же `UNIQUE constraint failed` (пытался архивировать его же активный
    счёт). Фикс: пропуск, если `old_account.person_id == new_person_id`.
    Заодно — чекбокс «пересчитать существующие номера» при смене формата
    (выключен по умолчанию, раньше пересчитывалось принудительно). Тесты —
    `tests/test_owner_account_archival.py`. pytest 234.
40. Чекбокс «Актуальные» на таблицах «Лицевые счета» (гараж/человек/общий
    список) — клиентский JS-фильтр, включён по умолчанию: скрывает
    архивные счета и полностью погашенные пени. Разметка —
    `data-archived`/`data-zero-penalty`, общий делегированный обработчик в
    `base.html`. pytest 234.
41. Предложения голосований от рядовых членов (см. принципы «Предложения
    голосований»). Тесты — `tests/test_proposals.py`. pytest 241.
42. Публичный комментарий к голосу (`VoteBallot.comment`/
    `VoteProposalBoardBallot.comment`, миграция `2b97a3a3dd71`, см.
    принципы «Электронное голосование»). pytest 244.
43. Голос правления по предложению расширен до всех 3 `VoteChoice`
    (добавлено «воздержался») — схема уже поддерживала, просто не
    принималось роутом. Решение по-прежнему `for > against`. pytest 245.
44. Расчётные счета вынесены на отдельную `/finance/bank-accounts` (были
    на общей странице «О кооперативе»). `Document.counterparty_id` FK
    (миграция `f14327a9e7f5`, SET NULL) — фильтр документов по контрагенту,
    заполняется вручную или автоматически при прикреплении файла к
    расходу/платежу/акту. Тесты — `test_document_counterparty.py`. pytest
    251.
45. Поиск+пагинация для таблиц карточки контрагента + раздел «Документы»
    там же — обобщённая разметка (`data-page-size`/`data-filter-text`/
    `data-table-filter-for`/`data-pager-for`/`data-no-results-for`), один
    делегированный обработчик вместо копии скрипта на каждую таблицу.
    Попутно найден и исправлен баг приоритета `|lower` в `data-filter-text`
    (применялся только к типу документа, не ко всей строке) — в двух
    местах. Тесты — `test_counterparty_detail_page.py`. pytest 254.
46. Кнопка «+ Добавить документ» на карточке контрагента — новый роут
    `counterparties.add_document`, фиксирует `counterparty_id` из URL (не
    из формы). pytest 257.
47. Убраны поля «Код подразделения»/«Кем выдан» из паспортных данных —
    миграция `6cfb7be7e25a`, затронуты форма/карточка/кабинет/ревизия ПД.
    Найден, но не тронут orphan-файл `app/persons_detail.html` (не
    подключается нигде). Тестов на паспортные поля не было — сквозная
    ручная e2e-проверка вместо нового файла. pytest 257 (без изменений).
48. Кнопка «В архив» для собственника доступна и с карточки гаража (не
    только с его собственной карточки), тот же роут `persons.archive_person`
    + поддержка `next`. Тесты — `test_person_archive.py`. pytest 261.
49. Багфикс чекбокса «Актуальные» (сессия 40): на страницах гаража/человека
    оставался старый серверный фильтр (`not is_penalty or charges`),
    придуманный ДО чекбокса — пени вовсе без начислений не долетали до
    HTML ни при каком состоянии галки. Фильтр убран в обоих местах —
    единая клиентская логика, как в эталонном `finance.member_accounts()`.
    `persons.statement()` (печать, нечем раскрыть скрытую строку)
    сохранил фильтр намеренно, с комментарием. pytest 262.
50. Кликабельное ФИО в автогенерируемых комментариях «унаследованный
    долг/переплата» — структурный `Charge.related_person_id`/
    `Payment.related_person_id` FK (миграция `0f446a1369f4`, см. принципы
    «Компенсирующие проводки») вместо парсинга ФИО из текста. Рендер —
    `comment_format.linkify_related_person()`, гейт `is_board()`. pytest
    264.
51. Выписка по лицевым счетам человека (`/persons/<id>/statement`)
    переработана — была по секции на счёт с построчной разбивкой по
    годам/платежам («слишком много лишней информации»), стала одна строка
    на счёт в общей таблице + отдельный блок «Пеня» с итогом + «Итого».
    Ссылка по номеру счёта ведёт на его карточку за деталями. pytest 268.
52. «Состав правления» → выпадающее меню «Правление» (Контакты/Созывы).
    Новая страница `/governance/contacts` (login_required, не board-only).
    pytest 273.
53. Отдельная печатная форма выписки (`.statement-print`, свой `@page`/
    шрифт, screen-блок скрыт в `@media print` и наоборот) — тот же приём,
    что `pd4/print.html`, избегает поломки Bootstrap-тем при печати. pytest
    274.
54. Печатная форма выписки получила официальную шапку (логотип +
    реквизиты кооператива) и блок подписей (председатель/бухгалтер по
    `is_chairman`/`is_accountant`, линия подписи, круглое место под
    печать). Пустые реквизиты/ФИО — секции рендерятся пустыми, без
    ошибки. pytest 276.
55. `Person.short_name` вынесено в общее свойство модели (было дублировано
    в `garages._owner_initials`). Уменьшен шрифт печатной формы. Убрано
    условное «/Задолженность» из подписи «Итого».
56. Расчёт пени для суда (`/persons/<id>/penalty-calculation`) —
    `compute_charge_penalty_breakdown()`: та же посуточная логика, что и
    `compute_charge_penalty`, но построчная раскладка по периодам
    (схлопывает дни с одинаковым остатком/ставкой/знаменателем).
    Принципиально ИГНОРИРУЕТ `penalty_calculated_through` — считает всю
    историю просрочки заново от первого дня, не только «новые дни с
    последнего запуска», иначе отчёт для суда отставал бы от реальной
    картины. Только читает БД. Отдельная печатная страница (шапка как у
    выписки, подпись только председателя). Ссылка с выписки — только если
    есть начисленная пеня. pytest 280.
57. Формула на печатной форме расчёта пени — с `text-muted` (нечитаемо на
    белом) на явный `.doc-note{color:#333}`, убран `no-print`. Кнопка
    «Расчёт» перенесена к таблице «Пеня». pytest 280 (без новых тестов).
58. Списание пени (`finance.write_off_penalty`) — компенсирующий `Payment`
    на всю сумму с обязательной причиной в комментарии (см. принципы
    «Компенсирующие проводки»), гейт `is_privileged()` (НЕ
    `roles_required(ACCOUNTANT)`/`is_board()` — BOARD и ACCOUNTANT на
    одном `ROLE_LEVEL`, второй пропустил бы рядового члена правления).
    Только для пенных счетов с отрицательным балансом. Тесты —
    `test_penalty_write_off.py`. pytest 286.
59. Печать (место под печать/подписи на выписке и расчёте пени) —
    трёхходовая отладка. Причина в итоге не в flexbox-геометрии (несколько
    попыток ушли впустую именно там), а в том, что общий `@media print`
    сброс в base.html (`body div, body main, body section { width:auto
    !important; margin:0 !important }`) матчил АБСОЛЮТНО любой `<div>`,
    включая блок подписи, и как `!important` побеждал специфичные, но не
    `!important` правила автора (specificity не имеет значения, когда одна
    сторона `!important`, другая нет). Настоящий фикс — `!important` на
    собственных width/margin блока подписи (более специфичный селектор +
    `!important` побеждает `!important`-сброс). **Урок**: при отладке
    print-only CSS сначала искать конкурирующие `!important`-сбросы в
    общих стилях, а не сразу менять layout-свойства конкретного блока; и
    расхождение экран/печать (screen-рендер flexbox иногда отличается от
    print-движка браузера) — самостоятельная категория багов, не ошибка в
    конкретных значениях. pytest 286 (CSS-only).
60. Зачёт средств между лицевыми счетами (`finance.transfer_member_account_funds`)
    — для исправления ошибочного разнесения платежа. Сохранил ограничение
    «тот же человек ИЛИ тот же гараж» из первой формулировки запроса, даже
    когда более короткая повторная формулировка его не повторила —
    решил не убирать предохранитель без явного запроса. Компенсирующая
    проводка (см. принципы), `related_person_id` на обеих записях.
    Проверка «тот же человек/гараж» — на сервере, не только фильтром
    списка (иначе можно подставить чужой id прямо в POST). Доступ —
    `is_board()` (не `is_privileged()` — здесь ничего не теряется, только
    исправляется запись). Тесты — `test_member_account_transfer.py`.
    pytest 291.
61. Поддержка запятой как разделителя дробной части при вводе чисел (см.
    принципы «Разбор дробного ввода по локали») — `app.i18n.parse_decimal`/
    `parse_optional_decimal`, заменены все прямые `Decimal(request.
    form[...])` по всему проекту + 3 дублированных локальных
    `_parse_decimal`-хелпера; денежные/дробные `<input type="number">` →
    `type="text" inputmode="decimal"`; JS-превью получили
    `parseDecimalInput()` в `base.html`. Тесты — `test_parse_decimal.py`.
62. Убрана рудиментная кнопка «Пересчитать на дату» (`penalty.run`) на
    `/finance/penalty/` — на момент этой сессии `accrue_penalties(today)`
    ещё вызывалась тихо на каждом открытии дашборда/страницы (см. п. 66,
    убрано позже в этой же сессии), так что курсор всегда был уже
    доведён до сегодня раньше, чем форма рендерилась — запрос с прошлой
    датой всегда бил в `start > target_date` и ничего не начислял. Формы
    загрузки ставки ЦБ перенесены из-под таблицы ставок в левую колонку
    (попутная правка вёрстки).
63. Массовое действие «Пеня» (Списать/Удалить) — выпадающая кнопка рядом
    с таблицей счетов на `/persons/<id>` (в рамках одного человека) и
    `/finance/member-accounts` (по всему кооперативу), видна только при
    наличии непогашенной пени. «Списать» (`is_privileged()`) — тот же
    компенсирующий платёж, что у одиночного списания (сессия 58), сразу по
    всем подходящим счетам с одной причиной. «Удалить» (`is_chairman()`) —
    удаляет сами начисления без компенсирующей записи (история стирается
    без следа — потому и более строгая роль), с той же защитой от порчи
    зачёта (сессия 60), что у `delete_member_charge`. Общие циклы
    (`_write_off_penalties_bulk`/`_delete_penalties_bulk`) переиспользуются
    между person-scoped и cooperative-wide роутами.

    **Баг найден на реальном использовании** (не тестами — начальные
    тесты не комбинировали несколько начислений + платёж на одном счёте):
    `_delete_penalties_bulk` читает `account.charges` в локальную
    переменную ДО цикла удаления; после `delete()+flush()` эта коллекция
    не обновляется сама (см. принцип «Протухшие relationship-коллекции»)
    — `reallocate_member_charges()` читал её же заново, пытался вставить
    `ChargeAllocation` на уже удалённый `charge_id` → `IntegrityError`.
    Воспроизвёл детерминированно отдельным скриптом перед фиксом. Фикс —
    `database.db_session.expire(account, ["charges"])` сразу после flush,
    перед реаллокацией.
64. `PowerPhaseReading.raw_params` (временный сырой JSON, см. было в
    докстринге модели) убран — имена/масштаб полей `params` для POWCT
    подтверждены живым тестом на оборудовании заказчика: `day_kwh`/
    `month_kwh` (раньше вычислялись на лету из raw_params при каждом
    открытии страницы, теперь пишутся поллером как обычные колонки),
    плюс новые `sled_online` (статусный светодиод, `params.sledOnline`) и
    `switch_on` (состояние реле, `params.switches[0].switch`, с запасным
    `params.switch` для одноканальных прошивок).
    `electricity_monitor._parse_kwh_from_reading()` (перепарсинг raw_params
    через `ast.literal_eval` на каждый заход) удалена целиком. UI: бейдж
    «реле выкл», статус LED рядом с временем обновления.
65. Платежи контрагенту (`/counterparties/<id>`) — два независимых
    добавления к `CounterpartyPayment`: (а) `adjusts_bank_balance` (bool,
    default True) + чекбокс «Списать эту сумму со счёта сейчас» — платёж,
    вносимый задним числом для истории (деньги уже списались с реального
    счёта раньше), не должен повторно уменьшать `BankAccount.balance`;
    флаг запоминается НА платеже (не передаётся заново при каждой
    операции), чтобы правка/сторно того же платежа знали, был ли реальный
    эффект на баланс, который нужно отменять/переносить (см. принципы
    «Компенсирующие проводки» — тот же класс задачи, обратная сторона).
    (б) `bank_statement_line_id` FK — альтернатива прикреплению скана
    платёжки: сослаться на уже загруженную строку выписки (`direction ==
    "debit"`, ещё не привязанную к другому платежу — проверка на уровне
    роута). Заодно — плейсхолдер суммы долга и закрытие пустым полем в
    форме платежа (тот же приём, что у `finance.add_member_payment`).
66. Начисление пени перенесено с «тихо на каждом заходе на дашборд/
    `/finance/penalty/`» на ежемесячный cron (`scripts/accrue_penalty.py`
    + `.sh`, см. принципы «Крон-скрипты пени») — по прямой просьбе: начисление
    было неочевидно для правления и раздувало историю начислений почти на
    каждый день при частых заходах. Сама `accrue_penalties()` не менялась
    (по-прежнему считает посуточно, идемпотентна) — изменился только
    триггер вызова. Убраны 2 тихих вызова (`app/main.py` dashboard,
    `app/penalty.py` view()) и устаревший текст «пересчитывается
    автоматически». Тесты — `test_penalty_accrual.py` (сам расчёт +
    регрессия, что дашборд/страница пени больше не начисляют).
67. Дашборд (`/dashboard`, правление+) обогащён — было 5 статичных карточек
    без единого действия. Добавлены: карточка «Долги перед кооперативом»
    (сумма отрицательных балансов + число счетов, две агрегатные `GROUP
    BY account_id`, не цикл `balance()` по каждому счёту), ссылка на
    `/finance/member-accounts`; блок «Требует внимания» — переиспользует
    уже посчитанные в общем context-процессоре
    `pending_pd_count`/`pending_votes_count`/`pending_proposals_count`
    (без единого нового запроса), ссылки на соответствующие списки; блок
    «Недавняя активность» — последние 8 записей `AuditLog`, ссылка на
    полный журнал (`governance.audit_log`). По прямой просьбе — цена
    запросов сознательно не оптимизировалась (правление 3-5 человек, редкая
    одновременная работа, отдельный VPS только под сайт) — функционал
    предпочтён кэшированию. Тесты — `test_dashboard.py`.
</content>

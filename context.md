

> Это технический журнал сессий работы над проектом — что именно менялось в
> коде/моделях, зачем, какие миграции для этого нужны — для следующей сессии
> продолжения разработки. Актуальный список «что реализовано / что нет» для
> человека — в `README.md` рядом с этим файлом; этот файл дополняет его
> технической историей изменений по каждой сессии, в хронологическом порядке.


# Контекст задачи: Объединение страниц электричества и электросчёта

## Выполнено ✅

### 1. detail.html — вкл��дки
- Добавлены Bootstrap tabs: **Информация** и **Электричество и счёт** (объединены Electricity + Account)
- Вкладка Информация обернута в `{% if _active_tab != 'account' %}`
- Вкладка Electrical + Account объединены в `{% if _active_tab == 'account' %}`
- Номера счёта и баланс вынесены в начало вкладки

### 2. garages.py — добавлены маршруты
- `add_electricity_meter()` — добавить счётчик (перенаправление на tab='electricity')
- `add_electricity_reading()` — внести показания (перенаправление на tab='electricity')
- `update_account_number()` — обновить номер счёта (перенаправление на tab='account')
- `add_charge()` — добавить начисление (перенаправление на tab='account')
- `add_payment()` — добавить платёж (перенаправление на tab='account')
- `_current_meter()` и `_meter_history()` — хелперы для счётчиков
- Расширен `detail()` — загрузка current_meter, meter_history, readings, account, account_balance, charges, payments, fee_types

### 3. master_meter.py — новый blueprint
- `/electricity/master` — журнал показаний общего счётчика
- `/electricity/master/add` — добавить показания
- `/electricity/master/<id>/delete` — удалить запись
- Шаблон: `templates/master_meter/view.html`

### 4. Обновления в `__init__.py`
- Убран `import electricity`
- Добавлен `import master_meter`
- Убран `register_blueprint(electricity_bp)`
- Добавлен `register_blueprint(master_meter_bp)`

### 5. power.py — исправление
- Добавлено `reading_date=dt.date(year, month, 1)` в `add_master_reading()`

### 6. models.py — обновлена модель
- В `MasterMeterReading` добавлены поля: `reading_date: Mapped[dt.date]` и `amount: Mapped[Decimal | None]`

### 7. Удалено
- `app/electricity.py`
- `app/templates/electricity/view.html`
- `app/templates/finance/account_detail.html`
- `finance.accounts` (список лицевых счетов)
- `finance.account_detail`, `finance.update_account_number`, `finance.add_charge`, `finance.add_payment`
- Ссылки "Электричество" и "Электросчёт" из `cabinet/garages.html`

### 8. base.html — меню
- Убрана ссылка "Лицевые счета" (`finance.accounts`)
- Добавлен пункт "Общий счётчик" (`master_meter.view`)

---

## Выполнено в этой сессии ✅ (ещё одна доработка вёрстки вкладки "Электричество и счёт")
- Форма "Внести показания" перенесена в правую колонку, под блок "Номер счёта"/"Баланс".
- Форма "Зарегистрировать платёж" перенесена в правую колонку, под форму "Внести показания" (доступ по-прежнему только chairman/accountant).
- Форма замены счётчика/опломбировки стала модальным окном (`#replaceMeterModal`), открывается кнопкой с иконкой "✎" внутри таблицы "Текущий счётчик" (последняя строка таблицы).
- "Журнал показаний" и "Начисления" объединены в одну таблицу "Журнал показаний и начисления" с колонками: Дата, Показания кВт·ч, Тариф ₽/кВт·ч, Сумма, От кого, Дата оплаты, Сумма.

### Доработка: связь показаний и начислений перенесена на уровень БД (не только вёрстки)
Первая версия объединённой таблицы сопоставляла показания и платежи чисто позиционно через `itertools.zip_longest(readings, payments)` — по порядковому номеру в двух независимо отсортированных списках, без реальной связи в данных. По просьбе — заменено на настоящую связь в БД:
- **models.py**: `ElectricityReading` получила поле `tariff` (Numeric(10,4)) — тариф ₽/кВт·ч, применённый при расчёте `amount` именно для этого показания (фиксируется в момент внесения, не пересчитывается задним числом при последующих изменениях тарифа). `Charge` получила FK `reading_id → electricity_reading.id` (`ondelete="SET NULL"`, индексирован) и relationship `reading` (`back_populates="charge"`); у `ElectricityReading` — обратная связь `charge` (`uselist=False`, один к одному). Раньше начисление, созданное по показанию, было связано с ним только текстом в комментарии ("Начислено по показаниям от {date}") — теперь это настоящий внешний ключ.
- **garages.py, `add_electricity_reading()`**: при внесении показания теперь `db_session.flush()` сразу после `add(reading)`, чтобы получить `reading.id`; начисление создаётся с `reading_id=reading.id`; тариф сохраняется в `reading.tariff` независимо от того, посчиталась сумма или нет.
- **garages.py, `detail()`**: `ledger_rows` строится заново без `zip_longest` — каждое показание берёт своё начисление через `r.charge` (реальная связь), плюс отдельными строками добавлены ручные начисления без привязки к показанию (`c.reading_id is None`, любой вид взноса — с меткой вида в скобках) и платежи; всё сортируется по фактической дате (`sort_date`) от новых к старым. Убран неиспользуемый импорт `itertools.zip_longest`.
- **⚠️ Требуется пересоздание БД** при следующем запуске (`rm instance/coop.db && python seed.py`) — `Base.metadata.create_all()` не добавляет новые колонки/FK к уже существующим таблицам (в проекте нет Alembic, см. существующий "нюанс" в этом файле про смену enum DocumentType).
- Отдельная таблица "Платежи" на вкладке убрана из вёрстки; её данные (`charges`/`payments`/`fee_types`) по-прежнему передаются в шаблон из `detail()` на случай будущих доработок.
- Заодно найдены и добавлены отсутствовавшие в `i18n.py` ключи переводов для этой страницы: "Информация", "Электричество и счёт", "Лицевой счёт не создан." (были в разметке ещё с прошлой сессии, но без перевода на EN).


### detail.html — вкладка "Электричество и счёт" объединена
- Убрана отдельная вкладка `_active_tab == 'electricity'`, весь её контент перенесён внутрь `_active_tab == 'account'`.
- Порядок: номер счёта и баланс → журнал показаний (+форма) и начисления/платежи (+форма платежа) → информация о счётчике (текущий + история) + форма замены счётчика.
- Форма ручного начисления (charge) убрана с detail.html — вместо неё подсказка со ссылкой на новую страницу.

### Отдельная страница начисления на гараж
- Новый маршрут `garages.add_charge_page` (`GET/POST /garages/charges/add`, доступ BOARD) — выбор гаража из списка, вид взноса, год, сумма, комментарий.
- Шаблон `app/templates/garages/add_charge.html`.
- Ссылка "Начисления на гаражи" добавлена в base.html, меню "Правление", после "Гаражи".
- Старый маршрут `garages.add_charge(garage_id)` (POST `/garages/<id>/charges/add`) оставлен как есть — на detail.html теперь не используется, но не удалён (используется только программно/для обратной совместимости; можно удалить в будущем, если не нужен).

### Побочные находки — устранены
- `app/templates/finance/member_accounts.html` содержал мёртвую ссылку на `finance.accounts` (роут удалён ранее) — ссылка убрана.
- `app/templates/finance/accounts.html` — осиротевший шаблон удалённого роута `finance.accounts`, удалён из проекта.

### Финальные проверки — пройдено
- `py_compile` всех файлов `app/*.py` — ✅
- Jinja2-парсинг всех шаблонов `app/templates/**/*.html` — ✅ (все успешно распарсились)
- Статическая сверка всех `url_for(...)` в шаблонах и .py-файлах с реально определёнными маршрутами по всем blueprint'ам — ✅ (0 несовпадений)
- `app.create_app()` — не запускался в этой сессии (в окружении недоступны flask/sqlalchemy/сеть для их установки); рекомендуется прогнать вручную при следующем локальном запуске.
- Ссылок на `electricity.py` или `finance.accounts` (кроме уже устранённой выше) не найдено.
---

## Выполнено в этой сессии ✅ (/cabinet/garages — электроэнергия + оплата ПД-4)

### Электроэнергия в таблице лицевых счетов
- `cabinet.py: garages()` — для каждого гаража, помимо `MemberAccount`-ов, теперь собирается его лицевой счёт на электричество (`garage.account`, модель `PersonalAccount`) с балансом через `accounting.balance(garage)`; передаётся в шаблон как `electricity_by_garage: {garage_id: (account, balance)}`.
- `templates/cabinet/garages.html` — строка "Электроэнергия" добавлена первой в таблицу счетов каждого гаража (номер счёта, баланс), кликабельна → `garages.detail(tab='account')`. Таблица теперь показывается, если есть либо электросчёт, либо хотя бы один `MemberAccount` (раньше — только при наличии `MemberAccount`).

### Кнопка оплаты → /pd4/print
- Новая последняя колонка в таблице — иконка 💳 "Оплатить", показывается только если по счёту есть задолженность (баланс < 0).
  - Для member-счетов: `url_for('pd4.print_slips', account_id=acc.id)`.
  - Для электросчёта: `url_for('pd4.print_slips', garage_id=o.garage.id)`.
- `pd4.py: print_slips()` — расширен: `garage_id` в query/form → печать по электросчёту гаража (см. ниже); `account_id` в GET query → печать по одному конкретному member-счёту (раньше GET без параметров печатал сразу вообще все долги пользователя — так и осталось как поведение по умолчанию, если `account_id`/`garage_id` не переданы).
- Новая функция `pd4._print_electricity_slip(garage_id)` — доступ: правление или текущие собственники этого гаража; берёт `garage.account`, считает долг через `accounting.balance(garage)`, строит QR через новую `accounting.pd4_qr_payload_electricity()`, сохраняет `PD4Document`, рендерит `pd4/print.html`.
- **models.py, `PD4Document`**: добавлено поле `personal_account_id` (FK → `personal_account.id`, `ondelete="SET NULL"`) как альтернатива существующему `account_id` (FK → `member_account.id`), по образцу уже имеющегося в `Charge` паттерна `garage_id`/`account_id` — `CheckConstraint` `ck_pd4_document_target` требует ровно одно из двух. **Требует пересоздания БД** (см. предыдущую заметку про `rm instance/coop.db && python seed.py`).
- **accounting.py**: новая `pd4_qr_payload_electricity(coop, bank_account, garage, personal_account, amount)` — тот же формат ГОСТ Р 56042-2014, что и `pd4_qr_payload()`, но т.к. электросчёт общий на гараж (не привязан к конкретному человеку), поле "ФИО плательщика" — все текущие собственники гаража через запятую; "Назначение платежа" берёт название вида взноса из `FeeType` с `code="electricity"` (или "Электроэнергия", если такого вида нет).
- **pd4/print.html**: рендерит электрослип через лёгкий duck-typed адаптер `pd4._electricity_account_view()` (`type(...)`, по образцу уже использовавшегося в `cabinet.profile()` для pending-ревизий) — подставляет `.account_number/.person.full_name/.fee_type.name/.garage` под тот же интерфейс, что и `MemberAccount`, чтобы не трогать разметку квитанции. Кнопки "Скачать PDF" и "Назад к выбору" скрыты для электрослипов (флаг `electricity=True` в шаблоне) — путь `/pd4/print/pdf` пока рассчитан только на `MemberAccount` и не адаптирован под электросчета.

### Побутная находка — исправлена
- `pd4.py: select()` вызывал неопределённую `_is_privileged()` — упало бы с `NameError` при первом же заходе правления на `/pd4/` с непустым `q`. Добавлена реализация (`chairman`/`accountant` — видят все счета и строки "пеня"; рядовой `board` — нет), как и описано в собственном докстринге функции.

### Финальные проверки — пройдено
- `py_compile` всех файлов `app/*.py` — ✅
- Jinja2-парсинг всех шаблонов — ✅
- Статическая сверка всех `url_for(...)` (121 шт.) с реальными маршрутами — ✅ (0 несовпадений)
- Сверка всех `_()`-вызовов с русским текстом против словаря переводов в `i18n.py` — новых пробелов не найдено (добавлены "Оплатить", "Лицевой счёт на электричество для этого гаража не создан.", "Нет задолженностей — печатать нечего."); оставшиеся ~20 пропусков — старые, не по теме этой сессии (`master_meter.py`/`master_meter/view.html`, `cabinet/profile.html`).
- `app.create_app()` не запускался — в контейнере недоступны flask/sqlalchemy.

---

## Выполнено в этой сессии ✅ (FIFO-закрытие долгов + доработки формы платежа/модалок)

### Разнесение платежа по начислениям (от старых долгов к новым)
- **models.py**: новая модель `ChargeAllocation` (`charge_id`, `payment_id`, `amount`) — таблица целиком новая, `Base.metadata.create_all()` создаст её сама, миграция не требуется. `Charge` и `Payment` получили `allocations` (`cascade="all, delete-orphan"`).
- **accounting.py**: `charge_sort_date(charge)` — дата для сортировки/закрытия (дата показания, если начисление связано со счётчиком, иначе 1 января года начисления). `reallocate_garage_charges(garage)` — полностью пересчитывает разнесение всех платежей гаража по всем его начислениям заново (стирает старые `ChargeAllocation` и строит заново FIFO: старые начисления закрываются старыми платежами, остаток платежа идёт на следующее по старшинству открытое начисление). Идемпотентна и на первом же вызове доразносит уже существующую историю — отдельная миграция для данных не нужна. `charge_paid_amount(charge)` — сумма, уже закрытая по конкретному начислению.
- **garages.py**: `reallocate_garage_charges(garage)` вызывается после `flush()` в всех четырёх местах, где на гараж добавляется `Charge` или `Payment`: `add_electricity_reading`, `add_charge_page`, `add_charge`, `add_payment`.
- **templates/garages/detail.html**: в объединённой таблице "Журнал показаний и начисления" у каждого начисления теперь бейдж статуса — зелёный "оплачено", жёлтый "частично: N ₽", красный "не оплачено" (на основе `charge_paid_amount`).

### Форма "Зарегистрировать платёж" — значения по умолчанию
- Дата — сегодня (`today.isoformat()`).
- Плательщик — список собственников гаража отсортирован по дате последнего платежа (DESC, кто платил последним — наверху; кто ни разу не платил — внизу), по умолчанию выбран первый в этом списке.
- Сумма — текущая задолженность по счёту (`-account_balance`, если баланс отрицательный; иначе поле пустое).
- Всё считается в `garages.detail()` (`today`, `owners_sorted`, `default_payer_id`, `default_payment_amount`) и подставляется в шаблон через `value=`/`selected`.

### Мелкие правки
- Кнопка/заголовок замены счётчика: если счётчика ещё нет — "Добавить прибор учета"; если уже есть — "Заменить прибор учета" (раньше всегда было "Заменить счётчик / опломбировку").
- Все `<div class="modal-dialog">` без явного размера по всему проекту (`cooperative/view.html`, `counterparties/list.html`, `documents/_create_modal.html`, `finance/fee_types.html`, `finance/member_accounts.html`, `garages/detail.html` — модалка замены счётчика, `meetings/_create_modal.html`, `power/view.html` ×3) получили класс `modal-lg` (у Bootstrap 5 это ровно `max-width: 800px` на десктопе) — заголовки больше не переносятся на две строки. Модалки, где уже был `modal-lg`/`modal-xl`, не трогал.

### Финальные проверки — пройдено
- `py_compile` всех файлов `app/*.py` — ✅
- Jinja2-парсинг всех шаблонов — ✅
- Статическая сверка всех `url_for(...)` (121 шт.) с реальными маршрутами — ✅
- Сверка всех `_()`-вызовов с русским текстом против словаря переводов в `i18n.py` — новых пробелов нет (добавлены "оплачено", "частично", "не оплачено", "Добавить прибор учета", "Заменить прибор учета"); оставшиеся ~20 — старые, не по теме сессии (`master_meter.py`/`master_meter/view.html`, `cabinet/profile.html`).
- Мини-симуляция FIFO-алгоритма разнесения на синтетических данных (3 начисления, 2 платежа) — распределение и остаток долга совпали с ожидаемым.
- Новая таблица `charge_allocation` создаётся автоматически через `Base.metadata.create_all()` — миграцию запускать не нужно (в отличие от прошлых сессий, где менялись колонки/constraint-ы уже существующих таблиц).

---

## Выполнено в этой сессии ✅ (один платёж — одна строка с начислением)

По скриншоту: полностью оплаченное начисление всё равно давало две строки в "Журнале показаний и начисления" — начисление и платёж не были связаны в самой таблице, только совпадали по бейджу "оплачено"/сумме. Теперь используется уже существующая связь `ChargeAllocation` (из прошлой сессии):

- **garages.py, `detail()`** — для каждого начисления (и из показания, и ручного) собирается список `payments`: платежи (точнее, суммы из `ChargeAllocation`), которые его закрывают, через `charge.allocations` (payer/date/amount), отсортированный по дате платежа. Это одна и та же строка, что и начисление — отдельной строки на платёж больше нет.
- Отдельной строкой платёж теперь идёт, только если у него есть **неразнесённый остаток** (аванс/переплата — часть платежа, которая не пошла ни на одно начисление, потому что открытых долгов на неё не хватило). Такая строка помечена "аванс" в столбце "Сумма" (начисления).
- Если начисление закрыто несколькими платежами (или один платёж разбит между несколькими начислениями), в ячейках "От кого"/"Дата оплаты"/"Сумма" (платёж) выводится список — каждый платёж на новой строке внутри той же ячейки.
- **templates/garages/detail.html** — колонки "От кого"/"Дата оплаты"/"Сумма" (платёж) теперь рендерят `row.payments` (список), а не одиночные `row.payer`/`row.payment_date`/`row.payment_amount`.
- Добавлен перевод "аванс" → "advance payment" в `i18n.py`.

### Финальные проверки — пройдено
- `py_compile` — ✅, Jinja2-парсинг всех шаблонов — ✅, все `url_for` резолвятся — ✅, новых пробелов в переводах нет.
- Мини-симуляция на синтетических данных, повторяющих скриншот (показание 15.08.2026, начисление 60 ₽, платёж в тот же день на 60 ₽ от Egorov) — теперь одна строка с обоими бейджами; отдельно проверен сценарий переплаты (платёж 80 ₽ на начисление 60 ₽ → 20 ₽ остаются неразнесёнными).

---

## Выполнено в этой сессии ✅ (валидация показаний + правка последнего значения председателем)

### Запрет вводить показания меньше предыдущих
- **garages.py, `add_electricity_reading()`** — после вычисления `baseline` (последнее показание этого счётчика, либо `initial_reading`, если счётчик только что заменён/добавлен — эта логика уже была) добавлена проверка: если `reading_value < baseline` → показания не сохраняются, `flash` с danger и текущим значением baseline, редирект обратно на вкладку. Поскольку baseline уже считался per-meter (не по гаражу в целом), сценарий "счётчик поменяли — новый начинается заново" уже работал верно, требовалось только запретить ввод меньше него.

### Исправление последнего показания председателем
- Новый маршрут `garages.edit_last_reading(garage_id)` (POST, только `role == 'chairman'`) — находит последнее показание ТЕКУЩЕГО счётчика гаража, пересчитывает его относительно того же baseline (предыдущее перед ним показание либо `initial_reading` счётчика) и того же/актуального тарифа: та же валидация "не меньше baseline"; если у показания уже была связанная электро-начисление (`Charge` через `reading_id`) — сумма в нём обновляется; если начисления не было, а теперь появилась положительная сумма — начисление создаётся; если сумма стала `None` (например, исправили на равное baseline) — начисление удаляется (`garage.charges.remove(charge)`, cascade delete-orphan). После — `reallocate_garage_charges(garage)`, чтобы разнесение платежей по начислениям (FIFO) пересчиталось с учётом правки.
- **templates/garages/detail.html** — кнопка "✎ Исправить последнее показание" (видна только председателю и только если у счётчика есть хотя бы одно показание) открывает модалку с текущим значением показания и комментарием, предзаполненными из последнего показания; отправляет форму на новый маршрут.
- Новые переводы в `i18n.py`: сообщения об ошибке валидации (два варианта — при добавлении и при правке), "Исправить последнее показание", "Последнее показание исправлено.", "Некорректное значение показаний."

### Финальные проверки — пройдено
- `py_compile` — ✅, Jinja2-парсинг всех шаблонов — ✅, все `url_for` резолвятся — ✅, новых пробелов в переводах нет.
- Мини-проверка арифметики (Decimal) отдельно для отказа при показании ниже baseline и для пересчёта суммы при корректном показании — совпало с ожидаемым.

---

## Выполнено в этой сессии ✅ (аудит доступа + смена пароля + поиск в селектах + массовое начисление чекбоксами)

### Критические дыры в доступе — исправлено
Обнаружено по прямому репорту пользователя (`/persons/2`, `/garages/3` открывались рядовому члену, хотя это не его данные) + последующий аудит всех блюпринтов:
- `garages.list_garages()` и `garages.detail()` — были `@login_required` без проверки владения; теперь список — только правление (`@roles_required(RoleEnum.BOARD)`), карточка гаража — правление либо собственник (`_is_owner_or_board()`, новый общий хелпер `_is_board_role()`).
- `persons.list_persons()` и `persons.detail()` — та же дыра (весь список членов с балансами был виден любому залогиненному); теперь список — только правление, карточка — правление либо сам человек (`g.user.person_id == person_id`).
- Побочный эффект: ссылки на карточку со-собственника/контактного лица в `garages/detail.html` теперь кликабельны только для правления — для рядового собственника это просто текст (иначе клик на со-собственника вёл бы на чужую `/persons/<id>` и падал 403).
- `garages.photo_file()` — не проверял владение гаражом вообще; добавлена проверка через `photo.garage`.
- `garages.add_payment()` — UI показывал форму только председателю/бухгалтеру, но сервер пускал любого собственника; добавлена явная проверка роли.
- `finance.add_member_payment()` — то же самое (UI: board/chairman/accountant; сервер пускал держателя счёта). Добавлена явная проверка роли вместо `_can_view_member_account()` (та функция — про право *смотреть*, не про право *платить за другого*).
- `pd4.print_slips()` и `pd4.print_pdf()` — POST-ветки (для правления, печать по выбранным из `/pd4/` счетам) не проверяли `_is_board()` вообще; рядовой член мог POST-ом передать чужие `account_id` и получить чужую платёжку с ФИО/суммой долга. Добавлена проверка в обе.
- Проверены и признаны корректными (не трогал): `documents`/`meetings`/`cooperative` — намеренно открыты всем логином (верхнее меню, не «Правление»), все мутации там уже были на `roles_required`; `power`/`counterparties`/`master_meter` — уже были полностью на `roles_required(BOARD)`/`CHAIRMAN` или с внутренней проверкой `_can_view()`.

### Смена собственного пароля
- Новый маршрут `cabinet.change_password()` (POST, `@login_required`) — доступен любому пользователю для своей же учётной записи: проверка текущего пароля (`check_password_hash`), минимум 4 символа, совпадение подтверждения.
- Форма "Изменить пароль" в `cabinet/profile.html` — вынесена ЗА пределы блока `{% if person %}`, чтобы работать и для пользователей без привязанной карточки (например исходный `chairman` из `seed.py`).

### Панель "Доступ в систему" — перенесена
`persons/detail.html`: personal-data таблица теперь в левой колонке (`col-md-7`), панель "Доступ в систему" (создание/сброс пароля/смена логина/включение доступа — только для правления) — в правой (`col-md-5`), в одном `row`. "Лицевые счета" оставлены ниже, во всю ширину.

### Поиск в длинных селектах
Общий, не зависящий от внешних библиотек JS-компонент в `base.html` — `enhanceSearchableSelect()` + `document.querySelectorAll('select[data-searchable]')` на `DOMContentLoaded`. Прогрессивно превращает `<select data-searchable>` в текстовое поле с выпадающим списком, фильтруемым по вводу; сам `<select>` остаётся в форме (просто `display:none`), значение синхронизируется — сервер ничего не знает про этот слой. Функция вынесена в `window.enhanceSearchableSelect`, чтобы её можно было вызвать вручную для элементов, добавляемых в DOM динамически (см. ниже). Применено к:
- `garages/_fields.html` + `garages/_owner_rows_script.html` — выбор члена кооператива в строке собственника (в т.ч. для строк, клонируемых на лету через `<template>` — `addRow()` теперь вызывает `enhanceSearchableSelect` на свежедобавленный select).
- `garages/detail.html` — выбор человека при добавлении собственника и при добавлении контактного лица.
- `finance/member_accounts.html` — выбор человека и гаража в модалке создания лицевого счёта.
- `meetings/_fields.html` — секретарь / председательствующий на собрании.
- `persons/accounts.html` — выбор человека при привязке учётной записи.

### Начисление на несколько/все гаражи разом
`garages/add_charge.html` — вместо одиночного `<select>` теперь чек-лист гаражей (внутри прокручиваемого блока) с текстовым фильтром по номеру/собственнику и кнопками "Выбрать все" (только среди отфильтрованных) / "Снять все", счётчик выбранных. `garages.add_charge_page()` (POST) — принимает `garage_id` списком (`request.form.getlist`), создаёт `Charge` и вызывает `reallocate_garage_charges()` отдельно для каждого выбранного гаража; после — один общий flash с количеством.

### Финальные проверки — пройдено
- `py_compile` всех `app/*.py` — ✅
- Jinja2-парсинг всех шаблонов — ✅
- Статическая сверка всех `url_for(...)` с реальными маршрутами — ✅ (0 несовпадений)
- Сверка всех `_()`-вызовов с русским текстом против словаря переводов — новых пробелов нет (добавлены все строки для смены пароля, чек-листа гаражей, массового начисления); проверено на дубликаты ключей в `i18n.py` — один найденный дубликат (`"Гаражи"`) убран.
- `node --check` на вынесенных инлайн-скриптах из `base.html` (поиск в селектах + существующая сортировка таблиц) — синтаксис ОК.
- README.md обновлён: убрано "смена пароля не реализована" из списка нереализованного, добавлены разделы про поиск в селектах и серверную проверку доступа к чужим гаражам/карточкам, обновлено описание страницы "Начисления на гаражи".


# Контекст задачи: Баланс по факту (не по ожиданиям), учёт контрагентов до конца, единый стиль кнопок, устранение дубля начислений

## Выполнено ✅

### 1. Баланс кооператива на дашборде — сумма расчётных счетов, а не ожидаемых поступлений
Раньше `accounting.cooperative_balance()` считал `sum(Payment) - sum(Charge)` по
всем лицевым счетам (гаражи + члены) — по факту это «сколько кооперативу
должны/переплатили», не «сколько денег реально есть». Переименовал старую
логику в `receivables_balance()` (осталась доступна, просто больше нигде не
вызывается), а `cooperative_balance()` теперь — `sum(BankAccount.balance)`
по всем расчётным счетам (то самое ручное поле из прошлой сессии). Единственный
вызывающий — `main.py:dashboard()`, менять больше было негде. Обновил подпись
под цифрой на дашборде и её перевод в `i18n.py`.

### 2. Модуль расчётов с контрагентами — начальный баланс, правка последнего платежа, сторно
**Модели (`models.py`):**
- `Counterparty.opening_balance` / `.opening_balance_date` — остаток на момент
  начала учёта в системе, если отношения с контрагентом велись раньше; тот
  же знак, что и `counterparty_balance()` (отрицательное — уже были должны).
- `CounterpartyPayment.reverses_payment_id` (self-FK, `ondelete="SET NULL"`) +
  `reverses`/`reversed_by` (relationship с `back_populates`, `remote_side`)
  — связь платежа с отменяющей его проводкой (сторно).

**`accounting.py`:**
- `counterparty_balance()` — добавлен `opening_balance` в формулу
  (`opening + paid - charged`; сторно уже входят в `paid` отрицательной
  суммой, отдельно обрабатывать не нужно).
- `reallocate_counterparty_expenses()` — переписана: сторно-платежи сами по
  себе в FIFO-разнесении не участвуют, их отрицательная сумма присоединяется
  к исходному платежу перед разнесением (`_effective(payment) = payment.amount
  + сумма всех сторно к нему`). Так исходный платёж и сторно остаются двумя
  отдельными видимыми строками в истории, но на статус «оплачено» у расходов
  влияют как единое целое (без этого правка не отражалась бы в
  `expense_paid_amount()` — исходный платёж продолжал бы аллоцироваться
  как ни в чём не бывало).
- `edit_counterparty_payment()` — новая. Правка суммы/даты/счёта/документа
  уже внесённого платежа: сначала возвращает старую сумму на старый счёт
  (если был), затем списывает новую сумму с нового — чтобы баланс счёта не
  «поплыл» при повторных правках. Пересчитывает разнесение.
- `reverse_counterparty_payment()` — новая. Создаёт `CounterpartyPayment` с
  отрицательной суммой и `reverses_payment_id`, возвращает деньги на счёт
  списания исходного платежа (`+= payment.amount`), пересчитывает разнесение.
  Исходный платёж не трогается.

**`counterparties.py`:**
- `edit()` / `create()` — принимают `opening_balance`/`opening_balance_date`.
- `edit_payment()` — новый роут, `POST /counterparties/<id>/payments/<id>/edit`.
  Разрешён **только для последнего** (по дате, среди не-сторно) платежа
  контрагента — та же логика безопасности, что раньше применили к удалению
  показаний счётчика: не переписываем историю задним числом. Блокирует правку,
  если платёж уже сторнирован или сам является сторно.
- `reverse_payment()` — новый роут, `POST /counterparties/<id>/payments/<id>/reverse`.
  В отличие от правки — разрешён для **любого** платежа (не только
  последнего): возврат денег могут обнаружить не сразу. Блокирует повторное
  сторно одного и того же платежа и сторно самой отменяющей проводки.
- `detail()` — вычисляет `last_payment_id` (среди не-сторно), передаёт в шаблон.

**Шаблоны:**
- `_fields.html` — добавлены поля начального баланса (используется и в
  `create`, и в `edit`).
- `detail.html` — полностью пересобран: карточка «Изменить» реквизиты
  (кнопка на странице контрагента, не только из списка), строка
  «Начальный баланс» в таблице реквизитов, в таблице платежей — колонка
  «Действия» с кнопками «Изменить» (только у `last_payment_id`) и «Сторно»
  (у любого ещё не сторнированного платежа), сторно-строки помечены бейджем
  и связаны текстом «сторно платежа от {date}» / «отменён проводкой от {date}»,
  у отменённых исходных платежей — зачёркнутый текст. Две новые модалки на
  платёж: `editPaymentModal{id}` (только для последнего) и
  `reversePaymentModal{id}` (для любого невачёркнутого).
- Заменил `_balance` → счётчик `counterparty_balance` был и раньше, тут не
  трогал; проверил цветовую индикацию (`text-danger`/`text-success`) не
  сломалась.

**Миграция** `53b80161436b_add_counterparty_opening_balance_and_.py` —
4 новых колонки (`counterparty.opening_balance`, `.opening_balance_date`,
`counterparty_payment.reverses_payment_id`) + именованный FK через
batch-режим (constraint назвался автоматически благодаря `naming_convention`
на `Base.metadata`, добавленному в позапрошлой сессии — руками имя задавать
не пришлось, в отличие от самого первого раза, когда мы на это натыкались).
Проверено на БД с реальными данными до миграции — данные не потерялись.

**End-to-end проверено:** контрагент с начальным балансом -2000 → расход
10000 → платёж 8000 (баланс -4000) → правка суммы платежа на 9000
(баланс -3000, баланс счёта скорректирован без «уплывания») → сторно
платежа (баланс -12000, деньги вернулись на счёт до исходного значения) →
повторная правка/повторное сторно того же платежа корректно блокируются.

### 3. Единый стиль кнопок — btn-primary на страницах, btn-outline-secondary только в диалогах
Скриптом (эвристика по глубине вложенности `<div>` от `class="modal fade"`
до соответствующего закрывающего `</div>`) прошёлся по 26 шаблонам:
- Вне модалок: `btn-outline-secondary` / `btn-outline-primary` / `btn-secondary`
  → `btn-primary` / `btn-outline-primary`.
- Внутри модалок: `btn-outline-secondary` оставлен как есть (кнопки «Отмена»),
  `btn-secondary` приведены к `btn-outline-primary`
  (в тексте задачи явно назван исключением только `btn-outline-secondary`).
- `btn-outline-danger` (кнопки удаления) не трогал — это другой смысловой
  цвет (предупреждение об опасном действии), в задаче речь не о нём.
- Два места в `garages/detail.html`, где кнопка «Отмена» в модалке почему-то
  не использовала стандартный `btn-outline-secondary` (а `btn-primary`/
  `btn-secondary`, что мой скрипт превратил бы в неотличимую от «Сохранить»
  синюю кнопку) — поправил вручную под общий паттерн «Отмена» = 
  `btn-outline-secondary`, как во всех остальных ~19 диалогах проекта.
Проверено: подсчёт `<div>`/`</div>` по каждому изменённому файлу (несовпадение
нашлось только в `power/view.html`, но оно не связано с этой правкой — я
менял только текст внутри `class="..."`, не сами теги; Jinja2-парсинг того же
файла проходит чисто); плюс живой smoke-тест — все затронутые страницы
отдают 200 после правки.

### 4. Устранение дубля: /garages/charges/add удалена, выбор гаражей — в /finance/mass-charge
Сравнил обе страницы: `add_charge_page()` — выбор гаражей чекбоксами +
фиксированная сумма (плюс единственная поддерживала начисление на
электричество гаража, но это и так уже отдельно можно сделать точечно на
карточке гаража через `garages.add_charge(garage_id)` — не потеряно).
`finance.mass_charge()` — только `RoleEnum.CHAIRMAN` (не весь `BOARD`, как у
старой страницы — оставил как есть, это осознанный выбор второй страницы,
её роль-гейт не трогал), но с тремя стратегиями расчёта (коэффициент/площадь/
земельный налог) и без выбора подмножества гаражей — всегда на все разом.

Перенос: в `finance.mass_charge()` добавлено чтение `garage_id` списком из
формы; если список пуст — поведение как раньше (все гаражи), если непуст —
фильтрует `Garage` по выбранным id (в т.ч. `total_area` для стратегии
«по площади» теперь считается от суммы площадей именно выбранного
подмножества, не всех гаражей). В `mass_charge.html` добавлен тот же
чек-лист с поиском/«Выбрать все»/«Снять все», что был у старой страницы.
`garages.add_charge_page()` и `garages/add_charge.html` удалены целиком;
пункт меню `base.html` перенесён в блок, видимый только председателю (там
же, где «Учётные записи» — раз доступ у `mass_charge` только для него), с
новым текстом «Массовое начисление» вместо «Начисления на гаражи».

End-to-end проверено: старый роут — 404; начисление с выбором одного
гаража из двух — создаёт начисление только выбранному; начисление без
выбора (пустой чек-лист) — уходит на все гаражи, как раньше (обратная
совместимость).

### 5. README.md обновлён
- Раздел «Массовое начисление» — переписан с учётом объединения с бывшей
  «Начисления на гаражи» (выбор гаражей теперь необязателен и здесь).
- Раздел «Электроэнергия» — дополнен: поставщик выбирается из контрагентов
  (не отдельная форма), автосоздание расхода на показание, опциональная
  сразу-оплата со счёта, удаление только последнего неоплаченного показания,
  защита от дубля месяца. Убрано упоминание отдельной страницы «Общий
  счётчик» — она удалена.
- Раздел «Контрагенты» — переписан полностью: расходы/платежи/разнесение
  `FIFO`/статус оплаты, правка последнего платежа, сторно, акты сверки,
  начальный баланс. Убран старый пункт «баланс любого контрагента всегда 0»
  из «Что осознанно не реализовано» — теперь реализовано.
- «Баланс кооператива на дашборде» — обновлено под сумму расчётных счетов,
  со ссылкой на `receivables_balance()` для старого смысла, если понадобится.
- Добавлен пункт «Единый стиль кнопок» в «Общее».
- «Известные ограничения» — убрано устаревшее «end-to-end ни разу не
  прогонялся» (неверно уже начиная с сессии про Alembic) — переписано под
  факт: end-to-end гоняется регулярно через `app.test_client()`, но это не
  формальный набор автотестов.

### Финальные проверки — пройдено
- `py_compile` всех `app/*.py` — ✅
- Jinja2-парсинг всех шаблонов — ✅
- Статическая сверка всех `url_for(...)` с реальными маршрутами — ✅ (0 несовпадений)
- Сверка `_()`-строк со словарём переводов — все новые ключи добавлены;
  проверка на дубликаты ключей в `i18n.py` — новых дублей не внесено (11
  дублей в файле — все старые, унаследованные, не в рамках этой сессии).
- Alembic-миграция сгенерирована и применена на БД с реальными
  предмиграционными данными — данные не потеряны.
- End-to-end сценарии по каждому пункту (см. выше) — все прошли.

# Гаражи и электроэнергия

[← К оглавлению](../../README.md)

## Гараж — карточка и электричество
- Номер, площадь, приватизация земли, комментарий (правит только правление);
  собственники с долями владения (начисления по видам взносов автоматически
  распределяются пропорционально доле; если сумма долей ≠ 1 — жёлтый бейдж
  на карточке, но начисление всё равно проходит); контактные лица; фото с
  галереей-лайтбоксом (загружает и правит только правление)
- У каждого собственника в таблице — «Изменить долю», «Удалить
  собственника» (убирает только из владения этим гаражом, доли остальных
  пересчитываются) и «В архив» (полная архивация человека — см. «Люди и
  учётные записи» ниже — прямо с карточки гаража, без перехода на карточку
  человека; кнопка скрывается, если человек уже в архиве)
- **Сводка «Лицевые счета»** на вкладке «Информация» — электросчёт (один
  на гараж) и лицевой счёт каждого собственника по каждому виду взноса
  (взносы/налог зависят от доли владения, поэтому у каждого содольщика —
  свой отдельный `MemberAccount`, даже на один гараж), с текущим балансом
  по каждому. Рядовой собственник видит в этой сводке только СВОЙ баланс —
  баланс содольщика не его дело; правление и председатель видят всё
- **Счётчик электричества** — история замен (номер, дата установки, дата
  опломбировки, показания при установке, номер пломбы на счётчике и на
  вводном автомате); актуальным считается последний по дате установки;
  кнопка «Добавить прибор учёта» / «Заменить прибор учёта» (текст зависит от
  того, стоит ли уже счётчик) открывает модальное окно
- **Ввод показаний** — доступен собственнику гаража и правлению; новое
  показание не может быть меньше предыдущего *этого конкретного счётчика*
  (если счётчик заменили — точка отсчёта заново от его показаний при
  установке, старая история не мешает); председатель дополнительно может
  исправить именно последнее внесённое показание через отдельную модалку
  (если его ошибочно ввёл рядовой член) — сумма начисления по нему
  пересчитывается автоматически
- **Начисление за электричество** считается автоматически: (новое показание
  − предыдущее) × тариф, действующий на дату снятия показаний; создаётся
  сразу с привязкой к конкретному показанию (`Charge.reading_id`)
- **Ручное начисление отдельного вида взноса на выбранное подмножество
  гаражей** — теперь часть страницы «Массовое начисление» (см. раздел
  [«Финансы и справочники»](finance.md)): выбор гаражей чекбоксами необязателен —
  если ничего не отмечено, начисление, как и раньше, идёт на все гаражи.
  Отдельная страница «Начисления на гаражи» (`/garages/charges/add`)
  удалена — она дублировала «Массовое начисление», только без стратегий
  расчёта (по коэффициенту / по площади / земельный налог) и без выбора
  подмножества гаражей; обе возможности объединены в одном месте
- **Объединённый «Журнал показаний и начисления»** на одну таблицу: дата,
  показания, тариф, сумма начисления (с бейджем «оплачено» / «частично: N ₽»
  / «не оплачено»), и тут же — кто/когда/сколько заплатил именно по этому
  начислению. Разнесение платежей по начислениям — по-настоящему в БД
  (`ChargeAllocation`), не текстовым сопоставлением: `FIFO` в обе стороны —
  старые начисления закрываются старыми платежами; пересчитывается заново
  при каждом новом начислении или платеже на гараж. Переплата/аванс, для
  которых пока нет открытого долга, — отдельной строкой с пометкой «аванс»
- **Регистрация платежа** — доступна только председателю и бухгалтеру (не
  рядовому члену и не обычному правлению); по умолчанию подставляются:
  сегодняшняя дата, плательщик — тот из собственников гаража, кто платил
  последним (список отсортирован по дате последнего платежа, новые сверху),
  сумма — текущая задолженность по счёту

## Мониторинг электропитания (`/electricity/`) — eWeLink POWCT
- Отдельная подсистема от «Счётчика электричества» выше: та — помесячный
  ручной ввод показаний общего счётчика для начислений; эта — автоматический
  снимок текущей мощности/напряжения/тока по 3 фазам вводного щита с
  устройств Sonoff POWCT через облако eWeLink, только для мониторинга на
  сайте, в начислениях не участвует
- Подключение к eWeLink — официальный **OAuth2 Open API** (authorization
  code flow, приложение зарегистрировано и одобрено на dev.ewelink.cc).
  Раньше использовался неофициальный вход по email/паролю (как в сторонних
  клиентах, например Home Assistant SonoffLAN) — модерация заявки могла
  занять до нескольких дней и была сознательно отклонена при постановке
  задачи; после одобрения заявки модуль переписан на официальный флоу. Имена
  и масштаб полей `params` для POWCT (мощность/напряжение/ток, расход за
  сутки/месяц, статусный светодиод, состояние реле) подтверждены живым
  тестом на оборудовании заказчика — см. `app/ewelink/client.py`, там же
  пометки, какие ОСТАЛЬНЫЕ детали протокола ещё не подтверждены (формат
  подписи страницы авторизации в браузере, имя параметра family id, хост
  региона)
- Настройка в 3 шага, все — только председателю: 1) App ID/App Secret
  приложения с dev.ewelink.cc; 2) кнопка «Войти через eWeLink» — браузер
  уходит на страницу авторизации eWeLink и возвращается на
  `/electricity/ewelink/callback` (этот адрес, целиком, со схемой и
  доменом, нужно прописать как redirect URI в настройках приложения на
  dev.ewelink.cc — он же показан на странице мониторинга); 3) выбор дома
  (family) из списка, полученного после авторизации. Привязка 3 устройств
  к фазам (Фаза A/B/C) — отдельной формой там же. Секреты и токены
  хранятся в БД зашифрованными (переиспользован `app/bank_api/crypto`, тот
  же ключ, что и для интеграции с банком)
- Опрос устройств — cron-скрипт `scripts/poll_ewelink.py`, раз в минуту (см.
  [«Автоматизация (cron)»](../deployment.md)), не фоновый поток в веб-процессе
- На странице: карточка по каждой фазе (текущие Вт/В/А, онлайн/офлайн,
  расход за сегодня/за месяц, состояние реле и статусного светодиода),
  суммарная мощность и суммарный расход по всем фазам, график истории за
  выбранный период
- `PowerPhaseReading` хранит уже разобранные/масштабированные значения
  (power_w/voltage_v/current_a/day_kwh/month_kwh/is_online/sled_online/
  switch_on), не сырой JSON целиком — имена и масштаб полей params для
  POWCT подтверждены живым тестом на оборудовании заказчика

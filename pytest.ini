[pytest]
# -n auto — параллельный прогон (pytest-xdist, см. requirements-dev.txt):
# фикстуры в tests/conftest.py изолированы по воркерам (tmp_path_factory,
# mkstemp). Для отладки одного теста — `pytest -n 0 ...`.
addopts = -n auto
filterwarnings =
    ignore::DeprecationWarning
